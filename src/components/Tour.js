import React, { Component } from "react";
import { Marker, Places } from "google-maps-react";

export class Tour extends Component {
    render() {
        return (
            <div>Tour placeholder...</div>
        )
    }
}

export default Tour;